# KCG Open Tasks

Last updated: 2026-05-06 KST.

This is the active task ledger for the KCG launch candidate on `codex/kcg-launch-readiness-catalog-20260427`.

Do not record passwords, tokens, cookies, recovery codes, real account credentials, private customer data, or unapproved private documents in this file. Store only task status, owners, blockers, and verification notes.

## How To Use

- Status values: `todo`, `doing`, `blocked`, `user-only`, `done`.
- Priority values: `P0` public-launch blocker, `P1` current work quality gate, `P2` launch-quality improvement, `P3` optional later work.
- Owners: `codex`, `junyoung`, or `shared`.
- Move tasks to `done` only when the acceptance criteria and verification note are true.
- Keep production deploys, stable alias changes, `kcgold.co.kr` DNS cutover, robots/noindex release, and search indexing blocked until junyoung gives explicit public-launch approval.
- For meaningful site/admin/QA changes, update `docs/setup/CHANGELOG.md`, `docs/setup/CURRENT_HANDOFF.md`, and the package version before claiming the work is traceable.

## Risk Labels

| 위험도 | 뜻 |
| --- | --- |
| LOW | 문서 수정, 체크리스트 수정, 설명 보강 |
| MEDIUM | UI 문구 수정, 이미지 교체, 작은 화면 구성 수정 |
| HIGH | API 로직, 시세 계산, 관리자 기능, 고객 데이터, 배포 설정 |
| FORBIDDEN | 결제, 실시간 거래, 개인정보 과수집, 법률/세무 자동판단, 경쟁사 시세 수집 |

Future tasks should be easy to classify in this shape:

| 작업 | 위험도 | 누가 결정 | 실제 사이트 영향 | 되돌리기 쉬움 |
| --- | --- | --- | --- | --- |

## Active Tasks

| ID | Priority | Status | Owner | Task | Acceptance Criteria | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| KCG-TODO-001 | P1 | done | codex | Fix `/review` finding for admin QA password fallback. | Playwright and screenshot login helpers prefer explicit test password, then `ADMIN_PASSWORD`, then local demo fallback. | Passed: `npm run test:site`, `npm run screenshot:site`, `codex review --uncommitted` |
| KCG-TODO-002 | P1 | done | codex | Re-run `/review` on current uncommitted work. | `codex review --uncommitted` returns no actionable findings, or findings are recorded here. | Passed: no actionable correctness issue found after final review |
| KCG-TODO-003 | P1 | done | codex | Run branch-level review before backup, merge, or PR. | `codex review --base main` is run after the current working tree is intentionally ready for backup/merge review. | Passed 2026-04-30 KST: `codex review --base main` completed with exit 0 and no actionable output after earlier review findings were fixed. |
| KCG-TODO-004 | P0 | done | shared | Confirm real legal/business display information before public launch. | Temporary legal placeholders are replaced with the supplied business registration facts and `/admin/launch` no longer reports legal placeholder blockers. | `/admin/launch`, `npm run audit:site` |
| KCG-TODO-005 | P0 | user-only | shared | Configure final Vercel operating secrets before public launch. | Production `ADMIN_PASSWORD` and `ADMIN_SESSION_SECRET` are set in Vercel and no secret is committed to Git. A temporary pre-launch admin password was reset and verified on 2026-04-28 KST; rotate to a stronger final value before search/public launch. | Vercel env inspection, `/admin/login`, `/admin/launch` |
| KCG-TODO-006 | P0 | done | shared | Finish `kcgold.co.kr` and `www.kcgold.co.kr` Cafe24 DNS connection. | Vercel domains are added. Cafe24 DNS points to the exact records from `vercel domains inspect` and existing MX/TXT/SPF/DKIM records are preserved. | Completed 2026-04-29 KST: Cafe24 A records show `kcgold.co.kr 76.76.21.21` and `www.kcgold.co.kr 76.76.21.21`; `Resolve-DnsName` and `npm run check:external -- --strict-domain` pass DNS checks. HTTPS certificate issuance is tracked separately under `KCG-TODO-034`. |
| KCG-TODO-007 | P0 | user-only | junyoung | Explicitly approve public launch and search exposure. | junyoung approves production deploy, stable alias/domain cutover, and robots/noindex release after blockers are cleared. | `/admin/launch`, `/robots.txt`, `/sitemap.xml` |
| KCG-TODO-008 | P2 | todo | shared | Prepare product catalog real photos, price labels, and consultation wording. | Public `/products` remains consultation-first, no checkout/cart behavior appears, and placeholders are replaced with approved operating copy/assets. | `docs/setup/PRODUCT_OPERATIONS_CHECKLIST.md`, `npm run test:site`, `npm run screenshot:site` |
| KCG-TODO-009 | P2 | todo | shared | Decide whether paid data/API/chart sources are needed for production value. | Any production data source has documented terms, attribution, and no competitor scraping or unlicensed redistribution. | `docs/quality/data-source-compliance.md`, provider terms review |
| KCG-TODO-010 | P1 | done | codex | Keep admin launch screenshots out of default public artifacts. | Default `npm run screenshot:site` removes stale admin launch screenshots and captures them only when `KCG_INCLUDE_ADMIN_SCREENSHOTS=1` is explicitly set. | Passed: opt-in screenshot captured admin images; default screenshot removed them; `codex review --uncommitted` |
| KCG-TODO-011 | P1 | done | codex | Keep admin QA password env loading consistent with Next. | Playwright tests and screenshot capture load the same Next env files as the local server before reading `ADMIN_PASSWORD`. | Passed: `npm run test:site`, opt-in `npm run screenshot:site`, `codex review --uncommitted` |
| KCG-TODO-012 | P1 | done | codex | Record KCG company scope for site work. | Handoff and README state that KCG covers 순금·고금 매입, 순금/골드바 판매, B2C phone inquiry/trade consultation, and B2B bulk/corporate consultation. | `npm run audit:site` |
| KCG-TODO-013 | P1 | done | shared | Strengthen the consultation product catalog for gold bars, pure-gold sales, scrap-gold buying, silver items, and B2B inquiries. | Mock data and Supabase seed expose the same consultation-first product set, with image/price fields ready for later operation and no checkout/cart behavior. | `/products`, `/admin/products`, `npm run audit:site` |
| KCG-TODO-014 | P0 | done | shared | Prepare Cafe24/Vercel domain runbook and Vercel-side domains. | The runbook explains Vercel domain add/inspect, Cafe24 DNS record entry, MX/TXT preservation, noindex retention, and post-DNS verification. Vercel-side domains have been added; Cafe24 DNS remains user-only under `KCG-TODO-006`. | `docs/setup/DOMAIN_SUPABASE_MARKET_RUNBOOK.md`, `npx vercel domains inspect kcgold.co.kr`, `npx vercel domains inspect www.kcgold.co.kr` |
| KCG-TODO-015 | P0 | done | shared | Prepare Supabase production data setup. | Schema/seed are applied; Vercel production has `NEXT_PUBLIC_SUPABASE_URL` and server-only `SUPABASE_SERVICE_ROLE_KEY`; service-role/secret keys were never committed or pasted into chat. | Completed 2026-04-29 KST: SQL counts show `prices=8`, `announcements=2`, `products=20`; production deployment `dpl_8MiBBbh3UyUe4RoKqSfLd1vkcCHQ` reports `/api/health` `mode=supabase`; admin products renders 20 Supabase-backed products. |
| KCG-TODO-016 | P2 | blocked | shared | Assess KRX Open API or market-data contract before production use. | KRX data is not displayed as KCG live reference data until the authentication key, utilization approval, terms, commercial-use limits, and any Koscom/market-data contract requirement are confirmed. | KRX/Koscom approval evidence, `docs/quality/data-source-compliance.md` |
| KCG-TODO-017 | P2 | todo | shared | Revisit Metals.Dev paid upgrade only when traffic or chart needs justify it. | Gold API remains the default free reference source; Metals.Dev is enabled only with a KCG-owned key and a cost/benefit decision. | Vercel env inspection, `/api/health` |
| KCG-TODO-018 | P2 | done | codex | Save the ChatGPT campaign image prompt. | A prompt file exists and avoids left/right layout, price-table, blank-space, UI panel, or black-box instructions. | `docs/brand/campaign-image-prompts.md`, `npm run audit:site` |
| KCG-TODO-019 | P1 | done | codex | Re-run `/review` on this larger uncommitted workset before backup, merge, PR, or public deployment. | Review completes without actionable findings, or findings are fixed/recorded. | `codex review --base main` completed with exit 0 on 2026-04-30 KST; earlier long-running `/review` timeout is superseded for the current branch review gate. |
| KCG-TODO-020 | P1 | done | codex | Add a local browser dashboard for the launch TODO ledger. | `npm run tasks:dashboard` renders `output/kcg-open-tasks.html` from `docs/setup/OPEN_TASKS.md`, with filters for status, priority, owner, and keyword search. | `npm run tasks:dashboard`, `npm run audit:site` |
| KCG-TODO-021 | P1 | done | codex | Restore the desktop home price table overlay. | At 1440px desktop, the company price table overlaps the full-width campaign image on the left; the campaign image keeps viewport width and does not shrink into a half-width banner. | Playwright bounding-box check, `npm run test:site`, `npm run screenshot:site` |
| KCG-TODO-022 | P1 | done | codex | Tune the desktop home lineup-panel ratio and color. | At 1440px desktop, the price table has a visible left image margin, a narrower mid-left panel ratio, and darker opaque lineup colors closer to the benchmark image. | Playwright bounding-box check, `npm run test:site`, `npm run screenshot:site` |
| KCG-TODO-023 | P1 | done | codex | Apply approved brand and AI image candidates without copying private document photos. | Home, product, service, and visit-guide surfaces use approved optimized public assets; File-Hub contract/document photos remain outside the repo; prompt policy allows natural engravings while banning fake prices, fake logos, private document details, and identifiable faces without approval. | Passed: `npm run audit:site`, rendered audit on `http://127.0.0.1:3100`, `npm run test:site`, `npm run screenshot:site`, secret-string scan |
| KCG-TODO-024 | P1 | done | codex | Apply a stronger home first-slide image than the failed prompt result. | `public/campaign/kcg-brand-gold-bars-20260427-v4.webp` is the optimized public first-slide asset; the source-preserving PNG remains in `public/`; `kcg-main-desk-photo-20260427-v3.webp` remains a supporting slide; the retired `kcg-main-commerce-banner-20260427-v2.jpg` is not used. | `npm run test:site`, `npm run screenshot:site`, visual check |
| KCG-TODO-025 | P1 | done | codex | Fix repeated posted-time display and cramped market-reference cards on `/prices`. | The company price detail section shows the common posted time once, spot rows do not repeat per-metal update times, the automatic conversion section shows USD/KRW once, and 3.75g wording appears at section level rather than inside every card label. | Passed: `npm run test:site`, `npm run screenshot:site`, manual `/prices` screenshots |
| KCG-TODO-026 | P1 | done | codex | Remove the washed-out text-over-image treatment from the home campaign visual. | The desktop campaign image no longer uses a heavy white haze or large marketing copy overlay; the price table remains the working text surface and the right side reads as an image-led banner. | `npm run test:site`, `npm run screenshot:site`, manual home screenshot |
| KCG-TODO-027 | P1 | done | codex | Complete full-site mobile design polish and fixed CTA regression guard. | Mobile bottom contact bar has dedicated layout padding and does not cover key route decision content; PageIntro highlights are lighter; the home first slide crop is tuned; duplicate mobile market-reference cards are suppressed. | Passed: `npm run lint`, `npm run typecheck`, `npm run audit:site`, rendered audit on `http://127.0.0.1:3200`, `npm run test:site`, `npm run screenshot:site`, manual screenshots |
| KCG-TODO-028 | P1 | done | codex | Complete final navigation, company, price-section, and stable deployment pass. | Header uses the streamlined GNB `시세 / 상품·매입 / 서비스 / 회사소개 / 매장안내 / 공지`; `/company` exists; real legal info appears in company/footer surfaces; home uses four slides without a visible pause button; stable URL points to the new Vercel production deployment. | Passed: local and stable rendered audit 542 checks, local and stable Playwright 9 tests, `npx vercel inspect https://kcg-confirm-preview.vercel.app/` shows `dpl_HqFpMs96vyhL4iL2GXJJT1GVJkn4` ready |
| KCG-TODO-029 | P1 | done | codex | Apply browser comment feedback for `/prices`, `/services`, `/company`, and `/products`. | `/prices` starts with the company price table before guidance; `/services` starts with an image-led action block; `/company` is legal/company-fact first and does not show service cards; `/products` has tabs, product count, sort controls, current posted-price reference cards, and a desktop sticky promo rail without checkout/cart behavior. | Passed: local and stable rendered audit 582 checks, local and stable Playwright 9 tests, local and stable screenshots, `npx vercel inspect https://kcg-confirm-preview.vercel.app/` shows `dpl_35HScYjr9nq1Ae3qQ88fMRP2taLB` ready |
| KCG-TODO-030 | P0 | done | codex | Reset temporary production admin access for pre-launch editing. | Vercel production `ADMIN_PASSWORD` and `ADMIN_SESSION_SECRET` were replaced without committing secrets, the stable URL was redeployed, and `/admin/login` reaches `/admin/launch` with the approved temporary password. | Passed: `npx vercel env ls`, `npx vercel --prod --yes`, Playwright login check on `https://kcg-confirm-preview.vercel.app/admin/login` |
| KCG-TODO-031 | P2 | done | codex | Add an official TradingView chart surface without scraping data. | Market reference sections include a TradingView official widget with visible attribution; KCG does not store, cache, or republish TradingView data as company data. | Passed: `src/components/market/trading-view-widget.tsx`, `npm run audit:site`, `npm run test:site`, stable rendered audit 591 checks, stable Playwright 9 tests, TradingView iframe check on `/prices` |
| KCG-TODO-032 | P1 | done | codex | Add a repeatable external-services status check after reconnects. | `npm run check:external` checks the stable URL health, robots/noindex posture, empty pre-launch sitemap, and Cafe24 DNS A-record status without reading or storing secrets. | Passed: `npm run check:external`; DNS warnings remain expected until Cafe24 records are entered |
| KCG-TODO-033 | P1 | done | codex | Create a local no-secret working-tree backup for USB transfer. | A zip backup exists under `C:\Users\junyo\Documents\File-Hub\90_Backups\kcg-site` and excludes `.git`, `.env*`, `.vercel`, `.next`, `node_modules`, `output`, and Supabase temp metadata. | Created `kcg-site-workingtree-20260428-210137.zip`; tar listing check found 208 entries and no excluded secret/cache paths |
| KCG-TODO-034 | P0 | done | codex | Verify Vercel HTTPS certificate on `kcgold.co.kr` and `www.kcgold.co.kr`. | Both custom domains respond over HTTPS, `/robots.txt` still returns `Disallow: /`, and `/api/health` still reports `indexing: disabled`. | Completed 2026-04-29 KST after deployment `dpl_8MiBBbh3UyUe4RoKqSfLd1vkcCHQ`: `https://kcgold.co.kr/api/health`, `https://www.kcgold.co.kr/api/health`, and both HTTPS `/robots.txt` checks return 200 with `mode=supabase`, `indexing=disabled`, and `Disallow: /`. |
| KCG-TODO-035 | P1 | done | codex | Apply boss-written company mission and KCG intro without rewriting. | Public company page uses the latest boss-written KCG company introduction verbatim, including the provided KC Group ranking phrases, without Codex polishing or adding extra claims. Internal/private notes and unrelated draft material remain excluded. | Passed previously: `npm run audit:site`, `npm run test:site`; current refresh re-verifies after the 2026-04-30 full-intro/image update. Guardrails still block internal notes, private document images, and unprovided new superlative claims. |
| KCG-TODO-036 | P1 | done | codex | Align temporary posted prices and mobile product/catalog polish before the stable refresh. | Mock/seed posted-price defaults match the one-time 2026-04-30 benchmark values; production Supabase prices were manually updated; mobile product count/sort appears before mobile promo banners; product fallback images are less repetitive. | Passed: local and stable `npm run test:site`, local and stable `npm run screenshot:site`, `npm run check:external -- --strict-domain`; production deployment `dpl_GKF7Fw3qobbStuRBkTLsRtrxNCXq`. |
| KCG-TODO-037 | P1 | done | codex | Add safer KCG-owned/generated image set for company, service, jewelry buying, and B2B surfaces. | Generated images are saved in File-Hub AI generated storage and only approved final assets are copied to `public/`; no private document photos, fake logos, readable prices, customer testimonials, or identifiable faces are used. Product card images are less repetitive by category. | Passed 2026-04-30 KST: `npm run lint`, `npm run typecheck`, `npm run audit:site`, `npm run tasks:dashboard`, `npm run build`, `npm run test:site`, `npm run screenshot:site`, `npm audit --audit-level=moderate`, `git diff --check`, stable audit/test/screenshot on `https://kcgold.co.kr`, and deployment `dpl_A6PcFnLwgVMw8g7FiCM5qZFDUHXe`; public UI now uses optimized `.webp` variants while source PNGs remain available. |
| KCG-TODO-038 | P1 | done | codex | Normalize 대표이사 labels, public phone numbers, family links, and image roles before committing. | Public UI uses `대표이사`; 본사 전화 is `02-747-1807`; 매장 전화 is `02-747-1806`; `02-747-1802`, person-label `대표/대표자`, and title-style `골든타워 본사/성창빌딩 매장` do not appear in public source; family links include 다비스 다이아몬드, 다이아민족, and 다비스 다이아몬드 블로그; home/product images avoid overusing the white-glove advisor visual. | Verified in this finalization pass with source guardrails, local validation commands, git diff checks, and post-deploy checks on `https://kcgold.co.kr`. |
| KCG-TODO-039 | P1 | done | codex | Normalize public typography scale and make product catalog tabs filter locally. | Public route headings/body labels stay within the KCG typography scale; `/products` tabs update already-loaded products without App Router RSC refetch or product-detail prefetch; card images use constrained eager/lazy loading and Next image optimization instead of all-eager unoptimized loads. | Passed: `npm run lint`, `npm run typecheck`, `npm run audit:site`, `npm run tasks:dashboard`, `npm run build`, targeted Playwright `products tabs filter locally` and `public typography`, full `npm run test:site`, `npm run screenshot:site`, `npm audit --audit-level=moderate`, `git diff --check`, stable audit/test on `https://kcgold.co.kr`, and `npm run check:external -- --strict-domain`. Deployed as Vercel `dpl_CCZFgUuN8LK5xbHynJ8gSLH8w1AX`. |
| KCG-TODO-040 | P1 | done | codex | Optimize large generated public images and add asset-size guardrails. | Large generated PNG originals stay in `public/` for source preservation, but public UI and seed/mock defaults reference optimized WebP variants; audit fails if core optimized assets exceed the size guardrail. | Implemented 2026-04-30 KST: six generated assets re-exported to `.webp`, public references updated, `audit:site` image-size guardrail added, branch review passed. |
| KCG-TODO-041 | P1 | done | codex | Prepare product/catalog operating checklist for launch data entry. | A no-secret checklist explains `/admin/products` fields, category photo priorities, price wording rules, and pre-launch acceptance criteria. | `docs/setup/PRODUCT_OPERATIONS_CHECKLIST.md` |
| KCG-TODO-042 | P2 | done | codex | Prepare a concise launch briefing for management review. | A no-secret briefing explains site direction, benchmark rationale, current readiness, remaining real-data decisions, and API/search launch posture. | `docs/setup/LAUNCH_BRIEFING.md` |
| KCG-TODO-043 | P1 | done | codex | Add admin posted-price auto-fill and improve admin operation UX. | `/admin/prices` has a compact posted-price table, an explicit single ON/OFF switch for automatic price operation, lawful-source-only formulas, safe auto-publish thresholds, manual review fallback only for held items, Supabase schema for settings/suggestions/run timestamps, cron route guarded by secret auth, and no competitor scraping or random price offsets. `/admin`, `/admin/products`, and `/admin/announcements` are easier to scan. | Passed locally after the latest auto-switch refinement: `npm run lint`, `npm run typecheck`, `npm run audit:site`, `npm run tasks:dashboard`, `npm run build`, `npm run test:site`, `npm run screenshot:site`, `npm audit --audit-level=moderate`, `git diff --check`, and `npm run check:external -- --strict-domain`. An admin price auto-mode screenshot was captured at `output/screenshots/admin-prices-auto-desktop.png`. Latest refinement adds `check_interval_minutes`, `min_apply_change_won`, `max_auto_publish_change_percent`, `business_hours_only`, `last_checked_at`, and `last_auto_applied_at`. Vercel Hobby cron remains daily unless Pro/external scheduler is used. |
| KCG-TODO-044 | P0 | done | shared | Apply the latest auto-publish Supabase columns to the production database. | Junyoung completed Supabase CLI login, the repo was linked to project `ehmsqlfxxydnebzjfarr`, `supabase/schema.sql` was applied through `npx supabase db query --linked -f supabase/schema.sql`, and row-level verification confirms the default settings row is present. | Completed 2026-05-04 KST: `price_auto_settings` default row has `is_enabled=false`, `mode=manual_review`, `source=gold-api`, `check_interval_minutes=60`, `min_apply_change_won=500`, `max_auto_publish_change_percent=0.05`, and `business_hours_only=true`; `price_auto_suggestions` exists and contains rows. Automatic posting remains off until an operator turns it on in `/admin/prices`. |
| KCG-TODO-045 | P1 | done | codex | Refresh KCG banner/product imagery and organize old/new candidates for selection. | Home, service, company, catalog, product fallback, mock data, seed data, and social image references use the 2026-05-03 text-free commercial image set where stronger than the old repeated desk assets; old/current and new candidates are grouped under `public/image-options/2026-05-03`; docs record the selection rationale and remaining real-photo risk. | Passed: `npm run audit:site`, `npm run lint`, `npm run typecheck`, `npm run tasks:dashboard`, `npm run build`, `npm run test:site`, `npm run screenshot:site`, `npm audit --audit-level=moderate`, `git diff --check`; visual check of home/products/services/company screenshots |
| KCG-TODO-046 | P1 | done | codex | Add the KCG Codex quality environment: repo-local skill, review checklist, official-docs index, one-command QA, rendered audit, viewport screenshots, and fixed-UI guardrails. | `npm run qa:site` passes; rendered audit ends with `0 skipped`; screenshot output includes full-page and viewport artifacts; `KCG site quality weekly` automation exists without production deploy/search/secret permissions. | Completed 2026-05-04 KST: parser checks passed; `npm run qa:site` passed with rendered audit `896 checks, 0 skipped`, Playwright `18 passed`, screenshot artifacts regenerated, and `npm audit --audit-level=moderate` found 0 vulnerabilities. |
| KCG-TODO-047 | P1 | done | codex | Add traceable versioning and Proactive Launch Steward Review guardrails. | KCG has a changelog, package version, handoff version snapshot, proactive review checklist, and audit guardrails so future work records version/deploy/rollback status without junyoung having to invent the process each time. | Passed 2026-05-05 KST: `npm run audit:site`; `npm run qa:site` with rendered audit `927 checks, 0 skipped`, Playwright `19 passed`, screenshots, and npm audit `0 vulnerabilities`; `git diff --check` had line-ending warnings only. |
| KCG-TODO-048 | P1 | done | codex | Improve release trace reporting, market table readability, company hero consistency, TradingView chart height, and future Kakao inquiry readiness. | `npm run release:trace` reports version/branch/HEAD/deploy/rollback state; market reference rows are more readable; `/company` uses the same image-left compact visual header as other route tabs; TradingView widget gets a defined 26rem container height and rendered chart-content checks; optional Kakao/Naver inquiry URLs can be enabled from `siteConfig` without unfinished public buttons. | Passed 2026-05-05 KST: `npm run release:trace`, `npm run audit:site`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm run test:site`, `npm run screenshot:site`, `npm run qa:site` with rendered audit `957 checks, 0 skipped` and Playwright `19 passed`, `npm run tasks:dashboard`, `npm audit --audit-level=moderate`, and `git diff --check` with line-ending warnings only. |
| KCG-TODO-049 | P1 | done | codex | Add dedicated admin screenshot QA and v0.2.2 expert-panel evidence hardening. | Admin screenshots can be refreshed with `npm run screenshot:admin`; source audit requires the command, helper script, v0.2.2 changelog, and updated handoff. | This pass updates `package.json`, screenshot helpers, `audit:site`, changelog, handoff, QA report, and product/auto-price operations docs. |
| KCG-TODO-050 | P2 | done | codex | Document automatic-price scheduler choices for company reporting. | A no-secret brief explains Gold API/KCG formula flow, Vercel Hobby once-daily cron posture, Vercel Pro/external scheduler options, and user-only payment/approval decisions. | `docs/setup/AUTO_PRICE_OPERATIONS_BRIEF.md`; `npm run audit:site` guardrail. |
| KCG-TODO-051 | P1 | done | codex | Add a reusable gold-exchange operations product-audit pass. | KCG quality guidance now forces admin, price, product/매입, service, customer-response, and launch-readiness work to check actual staff/customer workflows before treating technical QA or visual polish as complete. | Passed 2026-05-05 KST: `npm run audit:site` and `git diff --check` completed; diff-check reported only existing LF-to-CRLF working-copy warnings. |
| KCG-TODO-052 | P1 | done | codex | Add v0.2.4 operations-product checklist and beginner status guide. | `operations-product-audit-checklist.md` separates technical, operations, and conversion QA; beginner status doc explains current version, branch, backup, actual-site reflection, risk labels, and copy-paste Codex requests. | `npm run audit:site`, `npm run release:trace`, `npm run tasks:dashboard` |
| KCG-TODO-053 | P0 | done | codex | Audit already-connected API behavior as part of current site QA. | Existing API surfaces are inventoried; company posted price vs API reference hierarchy, stale/failure/rate-limit fallback, update time display, and tests/error states are documented before the next serious launch-readiness claim. | Completed 2026-05-05 KST: `docs/quality/existing-api-integration-audit-2026-05-05.md` inventories Gold API, Metals.Dev, Google News RSS, TradingView, Supabase, Vercel Cron auto-price refresh, and external status scripts; package/changelog/handoff/status/audit guardrails updated. |
| KCG-TODO-054 | P1 | todo | shared | Replace placeholder catalog/product images after real KCG-designed gold-bar/product photos arrive. | Current placeholder/generated/representative images are listed with usage locations, and actual product photos replace customer decision-making product images without pushing the homepage price table down. 실제 제품 사진이 도착하면 이 항목을 먼저 재검토한다. | Image asset audit, `/products` desktop/mobile screenshots, `npm run screenshot:site` |
| KCG-TODO-055 | P2 | done | codex | Prepare infographic candidates for price-table reading, old-gold buying flow, and visit preparation. | Candidate infographic briefs define purpose, route, ratio, text/alt coverage, forbidden claims, and whether a real photo or placeholder is required before implementation. | Completed in v0.2.10 by turning the candidates into public DOM guidance blocks for `시세표 보는 법`, `고금 매입 절차`, and `방문 준비물`; passed `npm run qa:site`, `npm run screenshot:site`, and visual screenshot inspection. |
| KCG-TODO-056 | P1 | done | codex | Apply v0.2.6 expert-panel public/admin UI polish. | Home posted-price panel fits desktop first viewport better; market-reference rows are more readable; mobile products/services reveal actionable content sooner; admin price snapshot labels clearly distinguish buy/sell rows. | Passed before final deployment gate: `npm run lint`, `npm run typecheck`, `npm run build`, `npm run screenshot:site`, `npm run screenshot:admin`; full `audit:site`, `qa:site`, and external checks are required before completion. |
| KCG-TODO-057 | P1 | done | codex | Apply v0.2.7 admin light theme and automatic-price UX redesign. | `/admin` now uses a light operations-console theme; `/admin/prices` has clear automatic price ON/OFF state, pending button feedback, calculation result messages near the action, and manual/automatic screenshots; ambiguous labels such as `지금 자동 확인`, `드래프트`, and `버튼을 누르면 바로 저장됩니다` are removed. | Verified locally with `npm run lint`, `npm run typecheck`, `npm run audit:site`, `npm run build`, `npm run test:site`, `npm run screenshot:admin`, `npm run screenshot:site`, `npm run qa:site`, `npm audit --audit-level=moderate`; production deploy checks are reported in the final task response. |
| KCG-TODO-058 | P1 | done | codex | Fix admin automatic-price toggle persistence and make `/admin` a practical daily operations dashboard. | Root cause: the mode switch changed React state before native form submission, so hidden fields could submit the opposite mode. The mode form now submits the intended `nextIsAutoOn` values, `/admin` is rebuilt around daily operator tasks, and `/admin/prices` uses a clearer light operations layout. | Passed before deploy: `npm run lint`, `npm run typecheck`, `npm run audit:site`, `npm run build`, `npm run test:site`, `npm run screenshot:admin`, `npm run screenshot:site`, `npm run qa:site`, `npm audit --audit-level=moderate`, `git diff --check`; external audit/test after deploy are reported in final response. |
| KCG-TODO-059 | P1 | done | codex | Apply v0.2.9 operations QA guard for navigation, TradingView, price history, and stale manual prices. | `매장안내` now precedes `회사소개` in GNB, TradingView disclosure/fallback is clearer, price history and daily snapshots have a baseline storage path, and 24시간 이상 수동 등록이 없으면 자동시세가 다음 점검에서 ON으로 전환되는 guard가 추가됨. | Required verification: `npm run lint`, `npm run typecheck`, `npm run audit:site`, `npm run build`, `npm run test:site`, `npm run screenshot:site`, `npm run screenshot:admin`, `npm run qa:site`, `npm audit --audit-level=moderate`, deploy/external checks. |
| KCG-TODO-060 | P1 | done | codex | Apply v0.2.10 visual guidance refresh before public launch. | Home/product/service/about/company images move to the 2026-05-06 Graphite Desk + Seoul Retail + Human Consultation set; price-table reading, old-gold buying, visit preparation, and FAQ guidance reduce pre-call confusion; robots/noindex, posted-price hierarchy, and no checkout/trading boundaries remain unchanged. | Passed 2026-05-06 KST: local `npm run lint`, `npm run typecheck`, `npm run audit:site`, `npm run build`, `npm run test:site` (20 passed), `npm run screenshot:site`, `npm run qa:site` (rendered audit 1116 checks, 0 skipped), `npm audit --audit-level=moderate`, `git diff --check`, visual screenshot inspection; deployed as `dpl_2YX967sa6B8Wx6nXhr1vJy25CFp1`; live `SITE_AUDIT_URL=https://kcgold.co.kr npm run audit:site` passed 1116 checks, live `npm run test:site` passed 20 tests, and `npm run check:external -- --strict-domain` confirmed indexing disabled and robots blocked. |

## Review Policy

- Use `codex review --uncommitted` for the current working tree after meaningful local changes.
- Use `codex review --base main` before backup, merge, or PR review.
- Use `codex review --commit <sha>` only when a specific commit needs focused review.
- Do not treat `/review` as a replacement for tests, screenshots, source attribution checks, or human approval on legal, trading, credential, production, and launch decisions.

## Current Blockers Before Public Launch

- Business/legal information is now reflected from the supplied registration document, but junyoung should visually confirm spelling and addresses before search indexing.
- `kcgold.co.kr` and `www.kcgold.co.kr` now resolve to Vercel through Cafe24 A records and both custom domains work over HTTPS. Keep noindex/robots blocking until explicit public launch/search approval.
- Temporary production admin access has been reset for pre-launch editing. Before public launch/search exposure, rotate `ADMIN_PASSWORD` to a stronger final value and keep the value outside Git/docs/chat.
- Supabase production storage is connected and `/api/health` reports `mode=supabase`. Before public launch, still confirm real product prices, photos, 공임/margin rules, and final admin secret rotation.
- Production deploy, stable alias, robots/noindex release, and search indexing still require explicit junyoung approval.
